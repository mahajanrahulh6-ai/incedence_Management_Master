# Incidence Management

Starter scaffold for an incidence management project.

Quick start

1. Create and activate a virtual environment: `python -m venv .venv && source .venv/bin/activate`
2. Install dev dependencies: `pip install -U pip && pip install pytest`
3. Run tests: `pytest -q`

This repository contains a minimal `src/` package and example tests to get started.
