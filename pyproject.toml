[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "incidence_management"
version = "0.1.0"
description = "Incidence management example scaffold"
requires-python = ">=3.8"
dependencies = []

[tool.pytest.ini_options]
python_files = "tests/*.py"
